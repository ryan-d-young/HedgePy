{
    "definitions": {
        "resource": {
            "type": "object",
            "properties": {
                "class": {"type": "string"}
            }, 
            "required": ["class"], 
            "additionalProperties": true
        },
        "template": {
            "type": "object", 
            "properties": {
                "vendor": {"type": "string"}, 
                "endpoint": {"type": "string"},
                "start": {"format": "datetime", "type": "string"},
                "end": {"format": "datetime", "type": "string"},
                "resolution": {"format": "duration", "type": "string"},
                "resource": {"$ref": "#/definitions/resource"},
                "field": {"type": "array", "items": {"type": "string"}}
            }, 
            "additionalProperties": false
        }
    }, 
    "type": "object",
    "properties": {
        "common": {"$ref": "#/definitions/template"},
        "templates": {"type": "array", "items": {"$ref": "#/definitions/template"}}
    }
}
